# Agent Rules

- **Deployment Constraint:** Do NOT automatically deploy the website after making code changes. You must wait for explicit permission or a direct command from the user before running any deployment commands (e.g. `firebase hosting:channel:deploy`, `firebase deploy`, or `npm run deploy`).
